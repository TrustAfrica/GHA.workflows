name: Node.js CI - with cache

on:
  workflow_dispatch:
  push:

jobs:
  ci:
    name: run build process
    uses: TrustAfrica/GHA.workflows/blob/main/.github/workflows/gha.workflows.build-node.yml@main
